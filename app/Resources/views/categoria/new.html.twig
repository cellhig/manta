{% extends 'base.html.twig' %}

{% block body %}
    <h1>Crear categoría</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('categoria_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
